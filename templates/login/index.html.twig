{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <div class="navbar">
        <a href="#" class="logo"><img src="{{ asset('img/logo.svg') }}" class="Logo"/></a>
        <ul>
            <li><a href="{{ path('login') }}">Login</a></li>
            <li><a href="{{ path('userRegistration') }}" class="btn">Registro</a></li>
            <li><a href="#"><img src="{{ asset('img/carro.svg') }}" alt="" class="carrito"></a></li>
        </ul>
    </div>
    <div class="ContainerForm">
        <img class="imagen" src="{{ ('img/imagen.png') }}"/>
        <div class="formulario2">
            <h2 class="titulo">Inicia Sesión</h2>
    <form action="{{ path('login') }}" method="post" class="form">
        <div>
        <label for="username">Correo Electrónico</label>
        <input type="text" id="username" name="_username" placeholder="email@gmail.com" value="{{ last_username }}"/>
        </div>
        <div>
        <label for="password">Contraseña</label>
        <input type="password" id="password" name="_password" placeholder="Ingresa tu contraseña"/>
        </div>
        {# If you want to control the URL the user is redirected to on success#}
        <div class="boton"> 
            <input type="hidden" name="_target_path" value="/plantas"/> 
            <button type="submit" class="btn">Login</button>
        </div>
    </form>
   </div>
</div>
{% endblock %}