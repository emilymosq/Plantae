<form method="post" action="{{ path('app_plantas_delete', {'id': planta.id}) }}" onsubmit="return confirm('¿Seguro quieres eliminar esta planta?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planta.id) }}">
    <div class="new">
    <button class="btn-red">Eliminar</button>
    </div>
    
</form>
